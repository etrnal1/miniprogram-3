<!--pages/post-detail/post-detail.wxml-->
<view  wx:for="{{post}}" wx:key="index">
	<text>{{item.title}}</text>
	<text> 作者  :{{item.author}}</text>
	<view>
	<text>{{item.data}}</text>
	</view>
	<view>
		<image src="{{item.postImg}}" />
	</view>
	<text>{{item.content}}</text>
</view>
